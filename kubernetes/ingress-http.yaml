apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: billing-ingress-http
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt
spec:
  ingressClassName: nginx
  rules:
  - host: "billing.cypod.solutions"
    http:
      paths:
      - pathType: Prefix
        path: "/"
        backend:
          service:
            name: simple-bank-api-service
            port:
              number: 80
  tls:
  - hosts:
    - billing.cypod.solutions
    secretName: simple-bank-api-cert
